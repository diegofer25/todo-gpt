{
  "name": "todo-gpt",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "@types/node": "^16.x",
    "openai": "^3.3.0",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  },
  "dependencies": {
    "axios": "^1.4.0"
  }
}
